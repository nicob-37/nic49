plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

group = 'com.nic49.bot'
version = '1.0'

repositories {
    mavenCentral()
    maven { url 'https://repo.hypixel.net/repository/Hypixel/' }
}

dependencies {
    implementation 'net.dv8tion:JDA:5.3.0'
    implementation 'io.github.cdimascio:dotenv-java:3.0.0'
    implementation 'org.jetbrains:annotations:24.0.0'
    implementation 'ch.qos.logback:logback-classic:1.4.11'
    implementation 'com.google.cloud:google-cloud-translate:2.33.0'
    implementation 'com.google.code.gson:gson:2.14.0'
}

test {
    useJUnitPlatform()
}

jar {
    manifest {
        attributes(
                'Main-Class': 'com.nic49.bot.Main'
        )
    }
}

// Also ensure shadowJar is configured correctly
shadowJar {
    archiveBaseName.set('nic49')
    archiveClassifier.set('all')
    archiveVersion.set('1.0')

    manifest {
        attributes 'Main-Class': 'com.nic49.bot.Main'
    }
}